{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "overlay-capability",
  "description": "Permissions for the overlay window (read-only)",
  "windows": ["overlay"],
  "permissions": [
    "core:default"
  ]
}
